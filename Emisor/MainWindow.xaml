<Window x:Class="EmisorApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Panel del Emisor" Height="348" Width="350" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" 
        Topmost="True" ShowInTaskbar="False">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#CC333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BgBorder" Background="{TemplateBinding Background}" 
                                CornerRadius="4" RenderTransformOrigin="0.5,0.5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BgBorder" Property="Background" Value="#EE555555"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BgBorder" Property="Background" Value="#FF888888"/>
                                <Setter TargetName="BgBorder" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.92" ScaleY="0.92"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ToggleButton">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="BgBorder" Background="{TemplateBinding Background}" 
                                CornerRadius="4" RenderTransformOrigin="0.5,0.5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BgBorder" Property="Background" Value="#EE555555"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BgBorder" Property="Background" Value="#FF888888"/>
                                <Setter TargetName="BgBorder" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.92" ScaleY="0.92"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#D81E1E1E" CornerRadius="10" Grid.RowSpan="4"/>

        <TextBlock Text="✋" HorizontalAlignment="Left" VerticalAlignment="Top" 
           Margin="10,10,0,0" Padding="8" Background="Transparent" 
           FontSize="18" Foreground="White" Cursor="SizeAll" 
           MouseLeftButtonDown="DragHandle_MouseDown" Panel.ZIndex="99"/>

        <Button Content="❌" HorizontalAlignment="Right" VerticalAlignment="Top" 
                Grid.Row="0" Margin="0,5,5,0" Width="30" Height="30" FontSize="12" 
                Background="Transparent" Click="CloseButton_Click" Panel.ZIndex="99"/>

        <ToggleButton x:Name="VoiceSwitch" Grid.Row="0" Margin="15,10,15,5" Padding="5,2" 
              Width="160" Height="25" Content="Micrófono OFF" 
              Foreground="White" Background="#444" 
              Cursor="Hand" Click="VoiceSwitch_Click" FontSize="12"/>

        <WrapPanel x:Name="ButtonsPanel" Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,10">
            <Button Content="T" Tag="té" Click="ManualBtn_Click" Width="45" FontFamily="Arial Black"/>
            <Button Content="🔴" Tag="círculo" Click="ManualBtn_Click" Width="45"/>
            <Button Content="🔻" Tag="triángulo" Click="ManualBtn_Click" Width="45"/>
            <Button Content="❌" Tag="equis" Click="ManualBtn_Click" Width="45"/>
            <Button Content="🔷" Tag="rombo" Click="ManualBtn_Click" Width="45"/>
            <Button Content="🗑️" Tag="borrar" Click="ManualBtn_Click" Width="45" Background="#AA880000"/>
        </WrapPanel>

        <StackPanel Grid.Row="2" Margin="15,0,15,10">
            <TextBlock Text="Símbolos enviados:" Foreground="Gray" FontSize="12"/>
            <TextBlock x:Name="CurrentStateText" Text="[ Vacío ]" Foreground="#00FF00" FontSize="16" FontWeight="Bold"/>
        </StackPanel>

        <ListBox x:Name="LogConsole" Grid.Row="3" Margin="15,0,15,15" Background="#880F0F0F" Foreground="LightGray" 
         BorderThickness="1" BorderBrush="#333" FontFamily="Consolas" Padding="5"
         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,0,0,2"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>